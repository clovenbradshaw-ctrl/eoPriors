{
  "name": "eopriors",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An append-only ledger of observations about source material, compressed against a constructed exemplar basis. See SPEC.md.",
  "repository": {
    "type": "git",
    "url": "https://github.com/clovenbradshaw-ctrl/eoPriors"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "validate:batches": "node scripts/validate-batches.mjs",
    "build:projection": "node scripts/build-projection.mjs",
    "verify:determinism": "node scripts/verify-determinism.mjs",
    "bridge:fold": "node scripts/run-fold-bridge.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
